[package]
name = "integration_tests"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
tokio = { workspace = true }
utils = { path = "../utils" }
reqwest = { version = "0.13", features = ["json"] }
